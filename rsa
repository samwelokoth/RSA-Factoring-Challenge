import sympy

def find_prime_factors(n):
    factors = sympy.factorint(n)
    p, q = list(factors.keys())
    return p, q

# Example usage:
n = 3233
p, q = find_prime_factors(n)
print(f"p = {p}, q = {q}")
